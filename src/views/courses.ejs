<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Cursos - LearnStudy - Hub</title>
    <link rel="icon" type="image/png" href="/images/logo_sin_fondo.png">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link rel="stylesheet" href="/views/css/layout.css">
    <link rel="stylesheet" href="/css/tailwind.css">
    <link rel="stylesheet" href="/views/css/courses.css">

    <style>
        body, h1, h2, h3, h4, p, span, button, input {
            font-family: 'Inter', sans-serif !important;
        }
        /* Patrón de logos interactivos */
        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('/images/Logo_sin_fondo.png');
            background-repeat: repeat;
            background-size: 500px;
            opacity: 0.10; 
            z-index: -1;
            pointer-events: none;
            animation: floatingPattern 20s ease-in-out infinite;
        }

        @keyframes floatingPattern {
            0%, 100% { transform: translate(0, 0) scale(1); }
            50% { transform: translate(15px, 10px) scale(1.02); }
        }
    
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
            background-color: #e2e8f0;
            border-radius: 10px;
        }
    </style>
</head>
<body class="text-slate-800 bg-slate-50">
    <%- include('partials/_sidebar') %>
    <%- include('partials/_header') %>
    <main class="container">
        <div class="max-w-7xl mx-auto p-6 md:p-10">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-10 gap-4">
                <div>
                    <h1 class="text-4xl text-slate-900 font-extrabold tracking-tight">Mis Cursos</h1>
                    <p class="text-slate-500 mt-1 font-medium">
                        <% if (misAsignaturas.length > 0) { %>
                            Tienes <%= misAsignaturas.length %> cursos activos. <%= creditosInscritos || 0 %> unidades de crédito inscritas.
                        <% } else { %>
                            No tienes materias inscritas todavía.
                        <% } %>
                    </p>
                </div>
            </div>

        <% if (misAsignaturas.length === 0) { %>
            <div class="flex flex-col items-center justify-center py-20 border-2 border-dashed border-slate-200 rounded-3xl bg-white">
                <div class="bg-blue-100 p-6 rounded-full mb-4">
                    <i data-lucide="book-plus" class="text-blue-600 w-12 h-12"></i>
                </div>
                <h2 class="text-xl font-bold text-slate-700 mb-2">Comienza tu semestre</h2>
                <p class="text-slate-500 mb-8 text-center max-w-sm">
                    Aún no has inscrito asignaturas. Haz clic en el botón de abajo para ver el catálogo disponible.
                </p>
                <button onclick="toggleModal()" class="flex items-center gap-2 px-8 py-4 bg-blue-600 hover:bg-blue-700 text-white rounded-2xl font-bold shadow-lg shadow-blue-200 transition-all transform hover:scale-105 active:scale-95">
                    <i data-lucide="plus" class="w-5 h-5"></i>
                    Inscribir mi primera materia
                </button>
            </div>

        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% misAsignaturas.forEach(curso => { %>
                    <div class="course-card bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden relative transition-hover duration-300 hover:shadow-md">
                        <div class="h-2 w-full bg-blue-500"></div>
                        
                        <div class="p-6">
                            <div class="flex justify-between items-start mb-4">
                                <span class="px-2 py-1 rounded text-[10px] font-bold uppercase tracking-wider bg-blue-50 text-blue-700">
                                    <%= curso.facultad %>
                                </span>
                                <i data-lucide="book" class="text-slate-300 w-6 h-6"></i>
                            </div>
                            
                            <h3 class="text-xl font-bold mb-1 text-slate-800 tracking-tight"><%= curso.nombre_asignatura %></h3>
                            <p class="text-sm text-slate-400 mb-4 font-semibold uppercase tracking-widest"><%= curso.codigo %></p>
                            
                            <div class="mt-6 pt-4 border-t border-slate-50 flex justify-between items-center">
                                <span class="flex items-center gap-1 text-blue-600 font-bold text-sm">
                                    <i data-lucide="award" class="w-4 h-4"></i> <%= curso.creditos %> Créditos
                                </span>
                                <span class="text-xs font-bold px-3 py-1 bg-green-100 text-green-700 rounded-full">Inscrito</span>
                            </div>
                        </div>
                    </div>
                <% }); %>

                <button onclick="toggleModal()" class="border-2 border-dashed border-slate-200 rounded-2xl flex flex-col items-center justify-center p-10 hover:border-blue-300 hover:bg-blue-50 transition-all group bg-white/50">
                    <div class="bg-slate-100 p-4 rounded-full group-hover:bg-blue-100 transition-colors">
                        <i data-lucide="plus" class="text-slate-400 group-hover:text-blue-500 w-8 h-8"></i>
                    </div>
                    <p class="text-slate-400 mt-4 group-hover:text-blue-600 font-bold">Nueva materia</p>
                </button>
            </div>
        </div>
        <% } %>
    </main>
    <%- include('partials/_footer') %>
    <div id="courseModal" class="fixed inset-0 z-50 hidden flex items-center justify-center p-4">
        <div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onclick="toggleModal()"></div>
        <div class="bg-white w-full max-w-2xl rounded-3xl shadow-2xl z-10 overflow-hidden">
            <div class="p-8">
                <div class="flex justify-between items-center mb-6">
                    <h2 class="text-2xl font-extrabold text-slate-900">Catálogo de Asignaturas</h2>
                    <button onclick="toggleModal()" class="text-slate-400 hover:text-slate-600"><i data-lucide="x"></i></button>
                </div>

                <div class="relative mb-8">
                    <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 w-5 h-5"></i>
                    <input type="text" id="searchInput" onkeyup="filterCourses()" placeholder="Buscar por nombre o código..." 
                        class="w-full pl-12 pr-4 py-4 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-blue-500 outline-none font-medium">
                </div>
                
                <div class="grid grid-cols-1 gap-3 max-h-[400px] overflow-y-auto pr-2 custom-scrollbar" id="catalogList">
                    <% disponibles.forEach(asignatura => { %>
                        <div onclick="selectCourse(this, '<%= asignatura.id %>')" class="course-item group flex items-center justify-between p-4 border border-slate-100 rounded-2xl cursor-pointer hover:border-blue-200 hover:bg-blue-50 transition-all bg-white">
                            <div class="flex items-center gap-4">
                                <div class="bg-slate-100 p-3 rounded-xl group-hover:bg-blue-500 group-hover:text-white transition-colors">
                                    <i data-lucide="plus-circle" class="w-6 h-6"></i>
                                </div>
                                <div>
                                    <h4 class="font-bold text-slate-800 course-name"><%= asignatura.nombre_asignatura %></h4>
                                    <p class="text-xs text-slate-500 font-semibold uppercase tracking-tighter"><%= asignatura.codigo %> • <%= asignatura.creditos %> Créditos</p>
                                </div>
                            </div>
                            <div class="selection-check hidden"><i data-lucide="check-circle-2" class="text-blue-600 w-6 h-6"></i></div>
                        </div>
                    <% }); %>
                </div>

                <form action="/inscribir" method="POST" id="inscribeForm">
                    <input type="hidden" name="asignaturaId" id="selectedCourseId">
                    <div class="mt-8 flex gap-3">
                        <button type="button" onclick="toggleModal()" class="flex-1 px-6 py-3 border border-slate-200 rounded-xl font-bold text-slate-600 hover:bg-slate-50">Cerrar</button>
                        <button type="submit" id="btnInscribir" disabled class="flex-1 px-6 py-3 bg-blue-600 text-white rounded-xl font-bold opacity-50 cursor-not-allowed">
                            Confirmar Inscripción
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <script src="/views/javascript/courses.js"></script>
    <script>
        lucide.createIcons();
    </script>
</body>
</html>